name: worker

on:
  push:

jobs:
  column:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        hardly: ["confidence", "ourselves", "vain", "definite", "rumour"]
        heel: ["hundred", "enable", "weed", "sound"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar XE
